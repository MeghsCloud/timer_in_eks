apiVersion: v1
kind: Service
metadata:
  name: timer-app
spec:
  type: NodePort
  selector:
    app: timer-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30080
